# MatrixLab

**Интерпретатор языка матричных вычислений с поддержкой длинной рациональной арифметики**

[![C++](https://img.shields.io/badge/C++-17-blue.svg)](https://isocpp.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)
[![Статус](https://img.shields.io/badge/Статус-Завершен-success.svg)]()

## 📋 О проекте

MatrixLab — это полнофункциональный интерпретатор специализированного языка для работы с матрицами, разработанный в рамках учебного задания по дисциплине «Языки программирования».

Проект реализует собственную систему вычислений с матрицами, элементами которых являются рациональные числа с произвольной точностью (длинная арифметика для числителя и знаменателя). Язык использует **кембриджскую префиксную нотацию** (польская нотация со скобками), что обеспечивает однозначность парсинга сложных выражений.

## 🎯 Ключевые особенности

- **Полноценный интерпретатор** с парсером и лексическим анализатором
- **Длинная рациональная арифметика** — точные вычисления без потерь
- **24 встроенные операции и функции** для работы с матрицами
- **Поддержка переменных** с идентификаторами до 31 символа
- **Детектирование ошибок** с указанием строки во входном файле
- **Форматированный вывод** матриц с выравниванием столбцов

## 🧮 Поддерживаемые операции

### Базовые операции
- `(+ A B)` — сложение
- `(- A B)` — вычитание
- `(- A)` — унарный минус

### Матричные операции
- `(* A B)` — матричное умножение
- `(/ A B)` — матричное деление (A × B⁻¹)
- `(.* A B)` — поэлементное умножение
- `(./ A B)` — поэлементное деление
- `(transpose A)` — транспонирование
- `(det A)` — определитель матрицы
- `(inv A)` — обратная матрица

### Функции создания матриц
- `(zeros n m)` — нулевая матрица n×m
- `(ones n m)` — матрица из единиц n×m
- `(eye n)` — единичная матрица n×n
- `(linspace x0 x1 n)` — вектор с n элементами от x0 до x1

### Операции над матрицами
- `(vertcat A1 A2 ...)` — вертикальная конкатенация
- `(horzcat A1 A2 ...)` — горизонтальная конкатенация
- `(sum A)` — сумма всех элементов
- `(prod A)` — произведение всех элементов
- `(min A B)` — поэлементный минимум
- `(max A B)` — поэлементный максимум

### Служебные операции
- `(= var value)` — присваивание переменной
- `(disp A)` — вывод значения

## 📁 Структура проекта
```
MatrixLab/
├── src/ # Исходный код
│ ├── parser/ # Парсер и лексер
│ ├── matrix/ # Класс матрицы и операции
│ ├── rational/ # Класс рациональных чисел
│ ├── interpreter/ # Интерпретатор выражений
│ └── main.cpp # Точка входа
├── include/ # Заголовочные файлы
├── tests/ # Тестовые файлы
│ ├── input1.txt
│ └── output1.txt
├── Makefile # Сборка проекта
├── CMakeLists.txt # Альтернативная сборка
└── README.md # Документация
```

### Требования
- Компилятор C++17 (g++ 7+, clang++ 5+, MSVC 2017+)
- CMake 3.10+ (опционально)
